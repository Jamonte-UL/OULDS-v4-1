{
  "name": "@oulds/components",
  "version": "0.1.0",
  "description": "Web Components for the Oulds design system",
  "main": "dist/index.cjs.js",
  "module": "dist/index.js",
  "types": "dist/types/index.d.ts",
  "files": ["dist", "loader", "custom-elements"],
  "scripts": {
    "build": "stencil build",
    "dev": "stencil build --dev --watch",
    "test": "stencil test --spec --e2e",
    "lint": "tsc --noEmit"
  },
  "dependencies": {
    "@oulds/tokens": "workspace:*"
  },
  "devDependencies": {
    "@stencil/angular-output-target": "^0.8.0",
    "@stencil/core": "^4.0.0",
    "@stencil/react-output-target": "^0.5.0",
    "@stencil/vue-output-target": "^0.8.0",
    "typescript": "^5.0.0"
  }
}
